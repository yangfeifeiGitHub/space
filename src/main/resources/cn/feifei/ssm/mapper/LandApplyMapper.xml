<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.feifei.ssm.mapper.LandApplyMapper">
  <resultMap id="BaseResultMap" type="cn.feifei.ssm.domain.LandApply">
    <id column="id" property="id" />
    <result column="applyDate" property="applyDate" />
    <result column="timeQuantum" property="timeQuantum" />
    <!--关联用地对象-->
  </resultMap>
  <delete id="deleteByPrimaryKey">
    delete from landapply
    where id = #{id}
  </delete>
  <insert id="insert" keyProperty="id" useGeneratedKeys="true">
    insert into landapply (siterinfo_id, applyDate, timeQuantum)
    values (#{siterinfo_id}, #{applyDate}, #{timeQuantum})
  </insert>
  <update id="updateByPrimaryKey">
    update landapply
    set siterinfo_id = #{siterinfo_id},
      applyDate = #{applyDate},
      timeQuantum = #{timeQuantum}
    where id = #{id}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap">
    select id, siterinfo_id, applyDate, timeQuantum
    from landapply
    where id = #{id}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select id, siterinfo_id, applyDate, timeQuantum
    from landapply
  </select>
</mapper>